## Overview

This template spins up a S3 bucket and a DynamoDB table which we use for configuring our Terraform backend. The S3 bucket contains our state file, and the DynamoDB table is used to implement a mutex lock on write access to the state file.